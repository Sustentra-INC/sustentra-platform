# S1 Adapters

Map current backend responses into Subsystem 1 view models here.

Adapter code is the boundary between backend implementation names and workpaper
semantics. React components should not read raw backend status fields directly.
