# S1 Constants

Store exact S1 copy, halt reasons, dimension names, allowed enum values, and
forbidden vocabulary checks here.

Constants should reflect the build spec and wireframes. Do not invent product
language in component files.
