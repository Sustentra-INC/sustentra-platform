# S1 Hooks

S1 hooks coordinate view-model state, keyboard behavior, filtering, grouping,
selection, and container states.

Hooks should consume S1 view models and audit intents rather than raw backend
DTOs.
