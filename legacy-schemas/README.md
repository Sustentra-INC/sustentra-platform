# Legacy Streamlit Demo Schemas

This folder preserves schema files copied from the ignored local Streamlit reference demo.

- Source snapshot: `demo_package/intern/schemas/`
- Copy destination: `legacy-schemas/copied_from_streamlit_demo/`

These files are preserved as historical references and should not be modified in place.
New production contracts live in `contracts/`.
